# ============================================================
# 建議版 .gitignore — 適用於本專案的四個 .NET 8 / WPF repo
# （RobotController / VisionController / SwallowBase / EIBG_Assemble / SerialController）
# 內容基於你原本的版本調整：拿掉 Node/Java/Maven 等不相干規則，
# 保留已經在用、驗證過有效的規則，補上 .NET/WPF 專屬的缺口。
# ============================================================

# ---- Visual Studio 核心輸出/快取（原本就有，保留） ----
.vs/
bin/
obj/

# ---- 建置產物在專案根目錄層級的情況（新增） ----
# 部分設定會把輸出放在專案資料夾下的 Debug/Release，不一定在 bin/ 底下
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Oo]bj/
[Oo]ut/
msbuild.log
msbuild.err
msbuild.wrn

# ---- MSBuild binary log（新增） ----
# 排查編譯問題常用 dotnet build -bl 產生，檔案可能很大，不該進版控
*.binlog

# ---- 使用者/機器相關設定（原本就有，保留） ----
*.user
*.suo
*.userosscache
*.sln.docstates

# ---- NuGet（原本有 packages/，新增套件輸出） ----
packages/
*.nupkg
*.snupkg
# 如果 nuget.config 裡有私有 feed 帳密，改用環境變數或 nuget.config.user，
# 不要把帳密提交進版控（本身檔案通常還是要 commit，只是內容不能含密碼）

# ---- 測試輸出（新增） ----
TestResults/
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml

# ---- CI / SDK-style 輸出（新增） ----
artifacts/

# ---- JetBrains Rider / ReSharper（原本只有 .idea/，補齊使用者層級檔案） ----
.idea/
*.DotSettings.user
_ReSharper*/

# ---- VSCode / Cursor（原本就有，保留） ----
.vscode/
.cursor/
.cursorignore

# ---- 作業系統產生的檔案（原本就有，保留） ----
.DS_Store
Thumbs.db

# ---- 一般編譯產物副檔名（新增，防止散落在非標準路徑） ----
*.dll
*.pdb
*.exe
# 上面 *.exe/*.dll 這種寫法在有子模組/第三方 binary 需要進版控時要小心，
# 若之後有「刻意要進版控的 DLL」（例如廠商 SDK 沒有 NuGet 版本），
# 用 git add -f 強制加入，或針對該路徑加白名單例外規則。

# ---- 敏感/機密設定（新增，目前專案雖然還沒用到，先預留） ----
*.pfx
*.snk
appsettings.*.local.json
local.settings.json
.env
.env.*

# ---- 大型媒體檔案（原本就有，保留，但依你的需求，如果 README 會放示意圖/GIF 可拿掉） ----
*.mp4
*.tiff
*.avi
*.flv
*.mov
*.wmv

# --- SKILL產物 ----
improve-codebase-architecture_files/
improve-codebase-architecture.html
