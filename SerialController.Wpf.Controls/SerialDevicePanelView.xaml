<UserControl x:Class="SerialController.Wpf.Controls.SerialDevicePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SerialController.Wpf.Controls"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="700">

    <DockPanel Margin="8">
        <!-- 頂部：連線按鈕 + 狀態訊息 -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="連線所有裝置" Padding="12,6" Margin="0,0,8,0"
                    Command="{Binding ConnectAllCommand}"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 主體：每台裝置一個區塊，垂直排列，可捲動（裝置數量可變） -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Devices}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FF3A5273" BorderThickness="1" Margin="0,0,0,8" Padding="10">
                            <StackPanel>
                                <!-- 裝置標題列：名稱 / 埠名 / 鮑率 / 連線狀態 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Margin="0,0,12,0"/>
                                    <TextBlock Text="{Binding PortName}" Foreground="#FF9FB0C7" Margin="0,0,12,0"/>
                                    <TextBlock Text="{Binding BaudRate}" Foreground="#FF9FB0C7" Margin="0,0,12,0"/>
                                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,4,0">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill" Value="Red"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                        <Setter Property="Fill" Value="LimeGreen"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock Text="{Binding StatusMessage}" Foreground="#FF9FB0C7" FontSize="11" VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- Holding Register 預覽：每格顯示位址 + 目前值 + 手動寫入輸入框 -->
                                <TextBlock Text="Holding Registers（可手動寫入）" FontWeight="Bold" Margin="0,0,0,4"/>
                                <ItemsControl ItemsSource="{Binding HoldingRegisters}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="90" Height="44" Margin="2" BorderBrush="#FF3A5273" BorderThickness="1">
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Address, StringFormat=#{0}}"
                                                               HorizontalAlignment="Center" FontSize="10"/>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <TextBox Width="40" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
                                                        <Button Content="寫入" Padding="2,0" Margin="2,0,0,0"
                                                                Command="{Binding DataContext.WriteRegisterCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
